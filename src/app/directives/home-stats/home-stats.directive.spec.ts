import { HomeStatsDirective } from './home-stats.directive';

describe('HomeStatsDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeStatsDirective();
    expect(directive).toBeTruthy();
  });
});
